scan:
   scanners:
      - vuln
      - secret
      - misconfig
   skip-dirs:
      - "**/node_modules"
      - "**/dist"
      - "**/out"
      - "**/build"
      - "**/cdk.out"
      - "**/coverage"
      - "**/.scratch"
      - "**/scratch"
      - "**/.git"
